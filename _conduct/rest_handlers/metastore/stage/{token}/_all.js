'use strict';

/**
 * Operations on /metastore/stage/{token}/_all
 */
module.exports = {
    
    /**
     * 
     * parameters: token
     * produces: application/json
     */
    get: function getAllStageProfileNames(req, res) {
        res.status(501).send('not implemented!');
    }
    
};
